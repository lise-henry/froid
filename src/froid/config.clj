(ns froid.config)

;; Defining global "variables" using to configure stuff

;; Points winned after each race, according to pos
(def points-winned [10 8 6 5 4 3 2 1])
;;(def points-winned [25 20 16 13 10 8 6 5 4 3 2 1])

;; same thing for xp
(def xp-winned (repeat 1))
